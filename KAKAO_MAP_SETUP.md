# 카카오맵 API 설정 가이드

## 1. 카카오맵 API 키 발급

1. [Kakao Developers](https://developers.kakao.com/)에 접속
2. 애플리케이션 생성
3. 플랫폼 설정에서 웹 플랫폼 추가
4. JavaScript 키 복사

## 2. 환경변수 설정

프로젝트 루트에 `.env` 파일을 생성하고 다음 내용을 추가:

```bash
# .env 파일
VITE_KAKAO_MAP_API_KEY=실제_발급받은_API_키
```

**중요**: Vite에서는 환경변수 이름이 `VITE_`로 시작해야 합니다.

## 3. 사용법

- 홈페이지에 접속하면 서울시청을 중심으로 한 지도가 표시됩니다
- 지도는 500px 높이로 설정되어 있습니다
- 서울시청 위치에 마커가 표시됩니다
- API 키가 설정되지 않은 경우 에러 메시지가 표시됩니다

## 4. 커스터마이징

`home.jsx` 파일에서 다음 부분을 수정하여 지도 설정을 변경할 수 있습니다:

- `center`: 지도 중심 좌표 (위도, 경도)
- `level`: 지도 확대 레벨 (1-14, 숫자가 작을수록 확대)
- 지도 크기: `className`의 `h-[500px]` 값

## 5. 주의사항

- `.env` 파일은 `.gitignore`에 추가하여 API 키가 공개되지 않도록 주의
- 실제 배포 시에는 환경변수나 서버 사이드에서 관리 권장
- 카카오맵 API 사용량 제한 확인 필요
- Vite 개발 서버 재시작 시 환경변수 변경사항이 반영됩니다

## 6. 문제 해결

### 지도가 안 뜨는 경우:

1. `.env` 파일에 올바른 API 키가 설정되었는지 확인
2. 브라우저 개발자 도구 콘솔에서 에러 메시지 확인
3. 카카오맵 API 키가 유효한지 확인
4. 개발 서버 재시작
